## Build-time configuration for the hostedat CLI binary.
## These values are compiled into the binary via -ldflags.

# Default server URL (used when no --server flag, env var, or config file is set)
DEFAULT_SERVER=https://hostedat.ditto.moe

# Site ID for deploying the docs site via `make deploy-docs`
DOCS_SITE=docs
